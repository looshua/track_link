import 'package:flutter/material.dart';



class BoxCanvas extends StatelessWidget {
  const BoxCanvas({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}